// Setup aur Basic Commands:
// git --version (Git check karne ke liye)
// git config --global user.name "[apka naam]" (User name set karne ke liye)
// git config --global user.email "[apki email]" (Email address set karne ke liye)
// git config --global --list (Sabhi settings check karne ke liye)
// git clone [URL] (GitHub se repo local machine par lane ke liye)
// git init (Nayi Git repository create karne ke liye)
// ls (Files dekhne ke liye)
// ls -a (Hidden files samet sabhi files dekhne ke liye)

// Working with Files:
// git status (Repository ka current status check karne ke liye)
// git add [file_name] (Specific file ko stage karne ke liye)
// git add . (Sabhi files ko stage karne ke liye)
// git commit -m "[message]" (Changes save ya commit karne ke liye)

// Remote aur Branching Commands:
// git remote add origin [URL] (Local repo ko GitHub se connect karne ke liye)
// git remote -v (Remote verify karne ke liye)
// git branch (Branch dekhne ke liye)
// git branch -m [new_name] (Branch ka naam change karne ke liye)
// git push -u origin main (Code ko GitHub par push karne ke liye)
// git push (Direct push ke liye)
// git checkout [branch_name] (Ek branch se doosri branch par switch karne ke liye)
// git pull origin main (GitHub se updates local machine par lane ke liye)
// git merge [branch_name] (Branches ko merge karne ke liye)
// git diff [branch-name] (Branches ke beech main kia Difference hai)

// Undoing Changes:
// git reset HEAD~1 (Last commit ko undo karne ke liye)
// git reset [hash_code] (Specific commit par wapas jaane ke liye)